@model Assignment.Models.Entities.KeSach

@{
    ViewData["Title"] = "Chi tiết Kệ sách";
}

<div class="container py-5">
    <div class="card shadow-lg border-0 rounded-4" style="max-width: 700px; margin: 0 auto; overflow: hidden;">
        <!-- Header -->
        <div class="card-header bg-primary py-4 border-0 text-center">
            <div class="bg-white text-primary rounded-circle d-inline-flex align-items-center justify-content-center mb-3" style="width: 80px; height: 80px; font-size: 2rem;">
                <i class="bi bi-bookshelf"></i>
            </div>
            <h4 class="mb-0 fw-bold text-white">@Model.TenKe</h4>
        </div>

        <div class="card-body p-4 p-md-5">
            <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex justify-content-between align-items-start p-3">
                    <div>
                        <div class="fw-bold text-muted small"><i class="bi bi-tag"></i> TÊN KỆ</div>
                        <div class="fs-6 mt-1">@Model.TenKe</div>
                    </div>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-start p-3">
                    <div>
                        <div class="fw-bold text-muted small"><i class="bi bi-geo-alt"></i> VỊ TRÍ</div>
                        <div class="fs-6 mt-1">@Model.ViTri</div>
                    </div>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-start p-3">
                    <div>
                        <div class="fw-bold text-muted small"><i class="bi bi-book"></i> DANH SÁCH SÁCH</div>
                        <div class="fs-6 mt-1">@Html.DisplayFor(model => model.Saches)</div>
                    </div>
                </li>
            </ul>

            <!-- Buttons -->
            <div class="d-flex gap-3 mt-4 pt-3 border-top">
                <a asp-action="Index" class="btn btn-outline-secondary w-50 py-2 fw-bold">
                    <i class="bi bi-arrow-left"></i> Quay lại
                </a>
                <a asp-action="Edit" asp-route-id="@Model?.MaKe" class="btn btn-warning w-50 py-2 fw-bold shadow-sm text-dark">
                    <i class="bi bi-pencil-square"></i> Chỉnh sửa
                </a>
            </div>
        </div>
    </div>
</div>